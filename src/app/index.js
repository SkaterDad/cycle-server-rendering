import repoList from './repo-list'

//This seems silly right now, but in the `dom-http-history` branch
//we will add some basic routing to `app`.
//What is important for this branch is that repoList returns {DOM, HTTP}.
const app = repoList

export default app
