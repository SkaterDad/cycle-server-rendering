import colorChanger from './color-changer'

//This seems silly right now, but in the `dom-http-history` branch
//we will add some basic routing to `app`.
//What is important for this branch is that colorChanger only returns a DOM sink.
const app = colorChanger

export default app
